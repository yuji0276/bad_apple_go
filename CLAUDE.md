# CLAUDE.md

## このプロジェクトについて

動画を ASCII アートとしてターミナルで再生する Go プログラム（Bad Apple プレイヤー）。
設計の詳細は [design.md](design.md) にある。作業前に必ず読むこと。

**これは学習用プロジェクトです。ユーザーが Go と動画処理を学ぶために自分で書きます。**

---

## 最重要ルール: 実装しない

コードを書くのはユーザーであって Claude ではない。

### やらないこと

- `main.go` / `decoder.go` / `ascii.go` / `player.go` への実装コードの書き込み・編集
- 「参考として」完成コードを提示すること（ユーザーはそれをコピーしてしまうので、学習にならない）
- ユーザーが詰まっている箇所の答えを、聞かれる前に先回りして出すこと
- 「ついでに直しておきました」的な自発的な修正

明示的に「書いて」「実装して」と頼まれない限り、ファイルは書き換えない。
頼まれた場合も、一度「自分で書いてみますか？」と確認してから。

### やること

- **質問に答える**: Go の文法、標準ライブラリの使い方、ffmpeg のオプションの意味
- **ヒントを出す**: 答えではなく、次に調べるべき場所・試すべきこと・考えるべき観点
- **レビューする**: ユーザーが書いたコードを読んで、バグ・設計上の問題・Go らしくない書き方を指摘する。
  ただし修正コードを書くのではなく「ここがこういう理由で問題」と指摘するに留める
- **デバッグを手伝う**: エラーの原因の切り分け。「どこまで動いているか確認するには何を出力すればいいか」を一緒に考える
- **確認コマンドを提案する**: `ffmpeg ... | wc -c` のような、動作を検証するワンライナー

### 説明の粒度

- 1〜3 行程度のコード片（シグネチャ、エスケープシーケンス、フラグの書式など）は出してよい
- 関数の中身がまるごと埋まるようなコードは出さない
- 「なぜそうなるのか」を必ず添える。動く呪文を渡すのは目的に反する

---

## 技術的な前提（design.md より）

会話の中で前提を取り違えないための要点。詳細は design.md を参照。

- **動画処理は全部 ffmpeg に任せる**。デコード・fps 正規化・縮小・グレースケール化は
  ffmpeg 側でやり、Go 側は「バイト列 → 文字」と「タイミング制御」だけを担当する
- ffmpeg から `-f rawvideo -pix_fmt gray` で受け取る。**1 ピクセル = 1 バイト（輝度 0..255）、
  1 フレーム = ちょうど `W*H` バイト、ヘッダも区切りもなし**
- 画像デコードライブラリや RGB→輝度の変換式は不要。リサンプリングも Go 側では書かない
- 文字セルは縦長なので `rows = cols × (srcH/srcW) × charAspect`（charAspect ≈ 0.5）。
  グリッドサイズは `scale` フィルタの引数なので **ffmpeg 起動前に確定させる**
- 依存追加は原則なし。`golang.org/x/term`（端末サイズ取得）だけが例外候補

### よくハマる箇所（先回りして答えを言わないこと）

ユーザーがこれらで詰まったときは、まず症状から原因を推測させる。

1. パイプからの読み出しに `io.ReadFull` を使う必要がある（`Read` だと途中までしか読めずフレームがズレる）— **ここが一番ハマる**
2. 終端は `io.EOF` と `io.ErrUnexpectedEOF` の 2 パターンある
3. ffmpeg プロセスの後始末（`Wait` / `Kill`）
4. `cmd.Stderr` の扱い
5. Ctrl-C 後にカーソルが消えたままになる問題
6. 輝度→文字の添字計算で `255` ではなく `len(ramp)` で割ると範囲外になる

---

## 環境

```
bad_apple/
├── design.md   設計メモ（正）
├── main.go     まだ空
├── go.mod      module asciiplay / go 1.25.3
└── test.mp4    640×480 / 30fps / 150 フレーム / 5 秒
```

- git リポジトリではない
- 段階 1〜3 の確認は `test.mp4` を使う（フル尺は最後まで取っておく）
- ビルド確認は `go build ./...` / 実行は `go run .`

---

## 進め方

design.md §7 の 5 段階に沿って進む。常に動く状態を保つのが狙いなので、
**先の段階の話を持ち出して現在の実装を複雑にしない**こと。

| 段階 | ゴール | 確認 |
|---|---|---|
| 1 | 1 フレームだけ ASCII で標準出力 | ランプとアスペクト比を目視調整 |
| 2 | 全フレームをループで読み、フレーム数を数える | `test.mp4` なら 150 |
| 3 | `\033[H` + タイミング制御 | アニメーションになる |
| 4 | 端末サイズ取得、フラグ、Ctrl-C 対応 | |
| 5 | Bad Apple フル尺 | |

ユーザーが今どの段階にいるか分からないときは、勝手に想定せず聞く。

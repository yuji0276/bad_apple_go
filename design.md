# Bad Apple ASCII プレイヤー 設計メモ

動画を ASCII アートとしてターミナルで再生する Go プログラムの設計。

**基本方針**: 動画処理の難しい部分（デコード・リサイズ・グレースケール化）は全部 ffmpeg に押し付け、
Go 側は **バイト列 → 文字の変換とタイミング制御だけ** にする。

---

## 1. 全体像

```
mp4 ──[ffmpeg 子プロセス]──> 生バイト列(stdout pipe) ──> Go ──> ターミナル
       デコード                 1フレーム = W*H バイト        文字割当
       fps正規化                1バイト = 輝度 0..255         タイミング制御
       グリッドサイズに縮小
       グレースケール化
```

### 核心のアイデア

ffmpeg の出力を「画像ファイル」ではなく **`rawvideo` / `pix_fmt=gray`** で受け取る。
すると出力は一切のヘッダもエンコードもない、ただの輝度バイトの連続になる。

- 1 ピクセル = 1 バイト（0 = 黒, 255 = 白）。すでに BT.601 で輝度計算済み
- 1 フレーム = ちょうど `W*H` バイト、区切り文字なし
- Go 側は「`W*H` バイト読む → 各バイトを文字に変換 → 出力」の繰り返しだけ

画像デコードライブラリも RGB→輝度の変換式も不要。
**さらに縮小も ffmpeg にやらせる**ので Go 側にリサンプリング処理が要らない。
ffmpeg が出す `80x30` はそのまま「80 桁 × 30 行」に 1:1 で対応する。

---

## 2. ffmpeg コマンドの設計

```bash
ffmpeg -hide_banner -loglevel error -i input.mp4 \
  -vf "fps=30,scale=80:30:flags=area,format=gray" \
  -f rawvideo -pix_fmt gray -
```

| 要素 | 役割 |
|---|---|
| `fps=30` | 可変フレームレート動画を固定 fps に正規化。これがないと再生タイミングが崩れる |
| `scale=W:H` | **文字グリッドのサイズをそのまま入れる**（§3 の計算結果） |
| `flags=area` | 縮小アルゴリズム。ピクセルの平均を取るので大幅縮小時のアンチエイリアスが自然。ASCII 化に最適 |
| `format=gray` | グレースケール化 |
| `-f rawvideo -pix_fmt gray` | ヘッダなしの生バイト出力 |
| 末尾の `-` | stdout に書き出す（= Go のパイプへ） |

### 手で動作確認

```bash
# 1フレーム分 = 80*30 = 2400 バイト出るか
ffmpeg -hide_banner -loglevel error -i test.mp4 \
  -vf "scale=80:30:flags=area,format=gray" -frames:v 1 \
  -f rawvideo -pix_fmt gray - | wc -c
```

---

## 3. グリッドサイズの決め方（文字のアスペクト比）

唯一の「動画的」な計算。ターミナルの文字セルは**正方形ではなく縦長**（およそ 幅:高さ = 1:2）。
素直に `80x60` にすると縦に 2 倍間延びする。

```
rows = cols × (srcH / srcW) × charAspect        charAspect ≈ 0.5
```

`test.mp4` は 640×480 (4:3)、80 桁なら:

```
rows = 80 × (480/640) × 0.5 = 30
```

`charAspect` は使っているフォント次第（0.45〜0.55 程度）なので **フラグで調整可能に**しておく。
目視で丸いものが丸く見える値に合わせる。

> **順序に注意**: グリッドサイズは `scale` フィルタの引数なので、**ffmpeg を起動する前に**確定させる必要がある。
> 「端末サイズ取得 → cols/rows 算出 → ffmpeg 起動」という流れになる。
> 再生中の端末リサイズに追従するには ffmpeg の再起動が要るので、v1 では非対応でよい。

---

## 4. 輝度 → 文字のマッピング

```go
const ramp = " .:-=+*#%@"   // 暗 → 明、10段階
idx := int(lum) * (len(ramp)-1) / 255
```

- 割り算に `255` を使うこと。`len(ramp)` で割ると `lum=255` で範囲外になる
- Bad Apple はほぼ 2 値なので短いランプで十分だが、10 段階あると輪郭のアンチエイリアスが効いて滑らかに見える

### 白黒反転について

暗い背景のターミナルでは「濃い文字 = 明るい」なので、**`輝度が高い → 密な文字`** が原色に忠実。
Bad Apple は白背景に黒シルエットの場面が多いため、忠実にやると
「`@` で埋まった画面にシルエットが穴として抜ける」絵になる。

好みが割れるので **`-invert` フラグ**を用意して両方見られるようにする（反転は `lum = 255 - lum` の 1 行）。

---

## 5. Go 側の構造

`package main` のまま、ファイルだけ分ければ十分。

```
main.go     フラグ解析、配線、シグナルハンドリング、後始末
decoder.go  ffmpeg 起動とフレーム読み出し
ascii.go    輝度→文字、フレーム→文字列
player.go   タイミング制御と描画ループ
```

### 型とシグネチャの案

```go
// decoder.go
type Decoder struct {
    cmd  *exec.Cmd
    out  io.ReadCloser
    W, H int
    buf  []byte        // W*H の使い回しバッファ（毎フレーム確保しない）
}

func NewDecoder(path string, w, h int, fps float64) (*Decoder, error)
func (d *Decoder) ReadFrame() ([]byte, error)  // 返り値は次回呼び出しで上書きされる
func (d *Decoder) Close() error

// ascii.go
type Renderer struct {
    W, H   int
    Ramp   string
    Invert bool
    sb     strings.Builder
}
func (r *Renderer) Render(pix []byte) string

// player.go
func Play(d *Decoder, r *Renderer, out *bufio.Writer, fps float64) error
```

`ReadFrame` がバッファを使い回すのは、30fps × 数千フレームで GC 圧を作らないため。
「返したスライスは次の呼び出しまで有効」という契約をコメントに書いておく。

### タイミング制御

素朴な `time.Sleep(frameDur)` は誤差が累積してだんだん遅れる。
**開始時刻を基準に絶対時刻で寝る**のが正解:

```
start := time.Now()
for i := 0; ; i++ {
    target := start.Add(time.Duration(i) * frameDur)
    // 読む → 遅れていれば描画をスキップ（読み込みはスキップしない）→ 描画
    // → time.Sleep(time.Until(target))
}
```

遅れている（`time.Until(target) < 0`）ときは
**フレームを読み捨てて描画だけ飛ばす**ことで、全体の再生時間が伸びるのを防ぐ。

### 描画

- **画面クリア（`\033[2J`）を毎フレーム打たない** — ちらつきの原因。
  代わりに `\033[H`（カーソルを左上へ）で上書きする。全セルを毎回書くので残像は出ない
- 1 フレーム分の文字列を `strings.Builder` で組み立て、`bufio.Writer` に書いて
  **フレームごとに 1 回だけ Flush**。細かく書くとティアリングする
- 開始時にカーソル非表示 `\033[?25l`、終了時に `\033[?25h` を必ず戻す

---

## 6. ハマりどころ

1. **`io.ReadFull` を使う** — パイプからの `Read` は要求より少ないバイト数を返す。
   `d.out.Read(buf)` では途切れたフレームを掴んで画面がズレる。必ず `io.ReadFull(d.out, d.buf)`。
   **一番ハマるのがこれ**
2. **終端の判定** — 動画の最後は `io.EOF`（きれいに終了）、中途半端に切れると `io.ErrUnexpectedEOF`。
   両方を正常終了として扱うか区別するか決めておく
3. **ffmpeg の後始末** — `defer cmd.Wait()` を忘れるとゾンビが残る。途中終了時は `cmd.Process.Kill()`
4. **stderr を捨てる** — `cmd.Stderr` を未設定（nil）にすれば `/dev/null` 行き。
   パイプに繋いで読まないと ffmpeg がブロックすることがある
5. **Ctrl-C の復旧** — `signal.Notify` で SIGINT を捕まえ、カーソル表示を戻してから終了。
   これをやらないと中断後にターミナルのカーソルが消えたままになる

---

## 7. 進め方

この順で作ると常に動く状態を保てる。

| 段階 | ゴール | 確認方法 |
|---|---|---|
| 1 | 1 フレームだけ ASCII で標準出力に出す | ランプとアスペクト比を目視調整。**ここを固めるのが最重要** |
| 2 | 全フレームをループで読み、フレーム数を数える | `test.mp4` なら 150 になるはず（`W*H` の倍数チェックにもなる） |
| 3 | `\033[H` + タイミング制御を入れて動かす | ここでアニメーションになる |
| 4 | 端末サイズ自動取得、フラグ整備、Ctrl-C 対応 | `golang.org/x/term` の `term.GetSize` が唯一入れる価値のある依存 |
| 5 | 本番の Bad Apple フル尺で流す | |

`test.mp4`（640×480 / 30fps / 150 フレーム / 5 秒）が段階 1〜3 の高速イテレーション用に最適。
フル尺は段階 5 まで取っておくと待ち時間を減らせる。

---

## 8. 拡張（やるなら）

- **音声**: `ffplay -nodisp -autoexit input.mp4` を別プロセスで同時起動するのが一番簡単。
  §5 の「開始時刻基準の絶対時刻タイミング」なら映像は壁時計に追従するので実用上ズレない。
  厳密な同期を追うと沼なので、まずはこれで十分
- **カラー**: `-pix_fmt rgb24` にすると 1 ピクセル 3 バイトになり、
  ANSI 24bit カラー（`\033[38;2;R;G;Bm`）が使える。
  ただし出力バイト数が 20 倍近くに膨らむので、色が変わったときだけエスケープを出す最適化が必要
- **事前レンダリング**: ASCII フレームをファイルにキャッシュしておき、再生時は ffmpeg なしで流す。
  配布や超軽量再生向け
